$('#resource').html('<%= escape_javascript(render "resource", :locals => {:topic_id => @topic.id}) %>');

$('.div_option').hide();
$('#resource .option').click(function optionDivsSlide(){

	var str = this.id.split('_');
	var id = str[str.length-1];

	if ($('#div_option_'+id).is(":visible"))
	{
		$('#div_option_'+id).slideUp();	
	}
	else
	{
		$('#div_option_'+id).slideDown();	
	}


});

// agregamos evento a los delete icon creados
$('#resource .delete_icon').click(function(){

	return confirm("Si elimina este objeto se eliminarán permanentemente todos sus sub-objetos");
});